import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  main: {
    flex: 1,
    alignItems: 'center',
    paddingTop: 20,
  },
  value: {
    color: 'black',
  },
});
export default styles;
